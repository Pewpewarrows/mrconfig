[${XDG_CONFIG_HOME:-$HOME/.config}/vcsh/repo.d/doom-emacs.git]
checkout = VCSH_BASE=~/.emacs.d vcsh clone -b develop https://github.com/hlissner/doom-emacs doom-emacs
commit = echo "No write access to this repository"
record = echo "No write access to this repository"
push = echo "No write access to this repository"
